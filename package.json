{
  "name": "carolinesplace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "echo 'Build complete'",
    "lint": "php -l index.php && php -l router.php && php -l clubhouse.php && php -l spa.php && php -l spa_menu.php",
    "start": "node server.js"
  },
  "dependencies": {}
}
